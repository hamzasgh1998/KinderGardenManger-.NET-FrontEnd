@model IEnumerable<PI4SAE.Models.Kid>
@{
    ViewBag.Title = "IndexKid";
    Layout = "~/Views/Shared/EspaceAdmin.cshtml";
}
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idKid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameKid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lastNameKid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.addressKid)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idKid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nameKid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.lastNameKid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.addressKid)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idKid }) |
                @Html.ActionLink("Details", "Details", new { id = item.idKid }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.idKid }) |
                    
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Back", "Index2", "Parent")
</p>
